# プロジェクト進捗: <プロジェクト名>

このファイルはユーザー向けの累積進捗レポートです。`MANAGER_INSTRUCTIONS.md`/`CODER_STATUS.md`と異なり圧縮・上書きせず、Stepごとに追記していきます。

**正本ではありません。** 目的・原則の正本は `DUAL_MODEL_OPERATION.md` の `Project Objective` / `Project Principle`、Coderの唯一の現在契約は `MANAGER_INSTRUCTIONS.md` です。このファイルはそれらの要約であり、多少ずれても構いません。

Stepは基本的に `Instruction ID` と1対1で対応します。プロジェクト開始時に、何Step（Instruction IDいくつ）で完成させるかの見積もりをここに残しておくと、後で照合しやすくなります。

## 最終目的（要約）

`DUAL_MODEL_OPERATION.md` の `Project Objective` を要約したもの。詳細・正本はそちらを参照。

```text
<要約>
```

## 確定方針（要約）

`DUAL_MODEL_OPERATION.md` の `Project Principle`、および、これまでに確定した主要な個別方針の要約。

- <方針1>
- <方針2>

## 全体STEP

| Step | Instruction ID | 内容 | 状態 |
| --- | --- | --- | --- |
| 1 | <ID> | <内容> | 完了 / 進行中 / 未着手 |

## 現在地

<現在どのStepにいるか、直近の判断、次の焦点を1〜2文で>

---

## Step <N>: <タイトル>

### 目的

<このStepの目的>

### 確定方針

<このStepで新たに確定した個別方針。プロジェクト全体の確定方針と重複する場合は書かない>

### 受入条件

1. <条件1>
2. <条件2>

### 完了内容

<Managerが、AcceptedとなったCODER_STATUS.mdの報告を圧縮して転記する>

---

<!--
Stepを追記していく際のルール:
- 古いStepの記述は削除・圧縮しない（STEPS.mdはFile Hygieneの圧縮ルールの例外）
- 詳細な設計議論はDUAL_MODEL_OPERATION.mdやプロジェクト固有の別ドキュメントへ逃がしてよいが、
  STEPS.md自体の各Stepの記録は残す
- 「最終目的」「確定方針」は要約であり、正本(DUAL_MODEL_OPERATION.md)と食い違っても
  致命的ではない。大きくずれてきたら見直す
-->
